import { useState } from 'react'
import axios from 'axios'
const UpdateAllUserInfo = () => {
    const backUrl = process.env.NEXT_PUBLIC_BACKEND_URL
  return (
    <div className="relative inset-0 bg-red-400 flex justify-center items-center m-20 p-10">
      <h1>UPdate AllUser info by admin</h1>
    </div>
  )
}

export default UpdateAllUserInfo
